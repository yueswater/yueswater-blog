---
title: "LeetCode 414. Third Maximum Number"
subtitle: "第三大的數"
lang: zh-TW
date: 2026-07-16
author: Anthony
categories: [LeetCode, Array]
description: "[題目連結](https://leetcode.com/problems/third-maximum-number/description/)"
image: ../../cover/LeetCode_Logo_black_with_text.svg
alternate-lang: "/dsa/posts/leetcode/0414-third-max-number/0414-third-max-number-en.html"
---

題目要求給定一個數字的陣列，回傳該陣列中第三大的數字；若不存在第三大的數字，則應回傳最大值，且時間複雜度必須為 $\mathcal{O}(n)$，資料限制為：

- $1 \le \texttt{nums.length} \le 10^4$
- $-2^{31} \le \texttt{nums[i]} \le 2^{31} - 1$

例如：

```
[3,2,1]
```

應回傳 `1`，因 `1` 為該陣列中第三大的數；又好比如

```
[2,2,3,1]
```

也應該回傳 `1`。但若給定

```
[1,2]
```

該陣列僅有兩個元素，不可能存在第三大，因此需回傳最大值，故回傳值應為 `2`。

## 想法

第一眼看到這題，直覺想用一個計數器邊走訪邊統計每個數字目前排第幾大。但這個做法有幾個問題：

1. 元素的排名會隨著後續走訪到的資料而改變，走訪當下無法直接確定最終名次。
2. 題目要求的是**不重複**的第三大值，遇到重複數字時不能重複計入排名。
3. 若每個元素都要重新與其他元素比較，容易形成巢狀迴圈，時間複雜度退化成 $\mathcal{O}(n^2)$。
4. 若不重複的數字不足三個，還得改回傳最大值。

換個角度思考，題目只在乎**前三大**，其實只需要維護一個最多三個元素的候選陣列：每遇到新數字，先排除重複值，再加入陣列並降冪排序；若候選值超過 3 個就移除目前最小的元素。走訪結束後，依候選陣列的元素個數判斷回傳第三大或最大值即可。

## 實作

三種語言的實作邏輯一致，即走訪時排除重複值，將新數字插入候選陣列並降冪排序，超過 3 個就捨棄最小值，最後依候選數量判斷回傳第三大或最大值。

::: {.panel-tabset}
## C

```c
#include <stdio.h>
#include <stdlib.h>

int thirdMax(int *nums, int numsSize)
{
    int maxNumbers[4]; // [1st max, 2nd max, 3rd max, buffer]
    int count = 0;

    // Traverse all elements
    for (int i = 0; i < numsSize; i++)
    {
        // Check if current num exists in maxNumbers
        // If it exists then skip
        int dup = 0;
        for (int j = 0; j < count; j++)
        {
            if (nums[i] == maxNumbers[j])
            {
                dup = 1;
                break;
            }
        }
        if (dup)
            continue;

        // Push to the end
        maxNumbers[count++] = nums[i];

        // Insertion sort descending: bubble the new element into place
        for (int j = count - 1; j > 0 && maxNumbers[j] > maxNumbers[j - 1]; j--)
        {
            int tmp = maxNumbers[j];
            maxNumbers[j] = maxNumbers[j - 1];
            maxNumbers[j - 1] = tmp;
        }

        // Prune to be 3
        if (count > 3)
            count--;
    }

    return count == 3 ? maxNumbers[2] : maxNumbers[0];
}
```

## C++

```cpp
int thirdMax(vector<int> &nums)
{
    vector<int> maxNumbers; // [1st max, 2nd max, 3rd max]

    // Traverse all elements
    for (size_t i = 0; i < nums.size(); i++)
    {
        // Check if current num exists in maxNumbers
        // If it exits then skip
        if (find(maxNumbers.begin(), maxNumbers.end(), nums[i]) != maxNumbers.end())
            continue;

        // Push and sort descending
        maxNumbers.push_back(nums[i]);
        sort(maxNumbers.rbegin(), maxNumbers.rend());

        // Prune to be 3
        if (maxNumbers.size() > 3)
        {
            maxNumbers.pop_back();
        }
    }
    return maxNumbers.size() == 3 ? maxNumbers[2] : maxNumbers[0];
}
```

## Python

```python
def thirdMax(self, nums: List[int]) -> int:
    maxNumbers = []  # [1st max, 2nd max, 3rd max]

    # Traverse all elements
    for i in range(len(nums)):
        # Check if current num exists in maxNumbers
        # If it exists then skip
        if nums[i] in maxNumbers:
            continue

        # Push and sort descending
        maxNumbers.append(nums[i])
        maxNumbers.sort(reverse=True)

        # Prune to be 3
        if len(maxNumbers) > 3:
            maxNumbers.pop()

    return maxNumbers[2] if len(maxNumbers) == 3 else maxNumbers[0]
```
:::